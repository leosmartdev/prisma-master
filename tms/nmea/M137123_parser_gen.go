// Code generated by parse_nmea; DO NOT EDIT
package nmea

import "fmt"
import "strconv"
import "strings"

// M137123 represents fix data.
type CoreM137123 struct {
	MessageID uint8

	RepeatIndicator uint32

	Mmsi uint32

	Spare1 uint32 //Supposed to be Unknown

	Longitude1 float64

	Latitude1 float64

	Longitude2 float64

	Latitude2 float64

	StationType uint32

	ShipAndCargoType uint32

	Spare2 uint32 //Supposed to be Unknown

	TxRxMode uint32

	ReportInterval uint32

	QuietTime uint32

	Spare3 uint32 //Supposed to be Unknown
}
type M137123 struct {
	VDMO
	CoreM137123
}

func NewM137123(sentence VDMO) *M137123 {
	s := new(M137123)
	s.VDMO = sentence
	return s
}

func (s *M137123) parse() error {
	var err error

	if MessageType(s.EncapData) != 23 {
		err = fmt.Errorf("message %d is not a M137123", MessageType(s.EncapData))
		return err
	}

	data := []byte(s.EncapData)

	//if len(data)*6 > 160 {
	//	err = fmt.Errorf("Message lenght is larger than it should be [%d!=160]", len(data)*6)
	//	return err
	//}

	s.MessageID = MessageType(s.EncapData)

	s.CoreM137123.RepeatIndicator = BitsToInt(6, 7, data)

	s.CoreM137123.Mmsi = BitsToInt(8, 37, data)

	s.CoreM137123.Spare1 = BitsToInt(38, 39, data)

	s.CoreM137123.Longitude1 = float64(BitsToInt(40, 57, data))

	s.CoreM137123.Latitude1 = float64(BitsToInt(58, 74, data))

	s.CoreM137123.Longitude2 = float64(BitsToInt(75, 92, data))

	s.CoreM137123.Latitude2 = float64(BitsToInt(93, 109, data))

	s.CoreM137123.StationType = BitsToInt(110, 113, data)

	s.CoreM137123.ShipAndCargoType = BitsToInt(114, 121, data)

	s.CoreM137123.Spare2 = BitsToInt(122, 143, data)

	s.CoreM137123.TxRxMode = BitsToInt(144, 145, data)

	s.CoreM137123.ReportInterval = BitsToInt(146, 149, data)

	s.CoreM137123.QuietTime = BitsToInt(150, 153, data)

	s.CoreM137123.Spare3 = BitsToInt(154, 6*(len(data)-1), data)

	return nil
}

func (s *M137123) Encode() (string, error) {
	var Raw string
	var Sbinary string

	if s.MessageID != 23 {
		err := fmt.Errorf("message %d is not a M137123", s.MessageID)
		return "", err
	}

	Raw = s.SOS + s.Talker + s.Format + ","

	if s.SentenceCountValidity == true {
		Raw = Raw + strconv.FormatInt(int64(s.SentenceCount), 10) + ","
	} else {
		Raw = Raw + ","
	}

	if s.SentenceIndexValidity == true {
		Raw = Raw + strconv.FormatInt(int64(s.SentenceIndex), 10) + ","
	} else {
		Raw = Raw + ","
	}

	if s.SeqMsgIDValidity == true {
		Raw = Raw + strconv.FormatInt(int64(s.SeqMsgID), 10) + ","
	} else {
		Raw = Raw + ","
	}

	if s.ChannelValidity == true {
		Raw = Raw + s.Channel
	}

	str := strconv.FormatInt(int64(s.CoreM137123.MessageID), 2)
	for len(str) < 6 {
		str = "0" + str
	}
	Sbinary = Sbinary + str

	str = strconv.FormatInt(int64(s.CoreM137123.RepeatIndicator), 2)
	for len(str) < 2 {
		str = "0" + str
	}
	Sbinary = Sbinary + str

	str = strconv.FormatInt(int64(s.CoreM137123.Mmsi), 2)
	for len(str) < 30 {
		str = "0" + str
	}
	Sbinary = Sbinary + str

	str = strconv.FormatInt(int64(s.CoreM137123.Spare1), 2)
	for len(str) < 2 {
		str = "0" + str
	}
	Sbinary = Sbinary + str

	str = strconv.FormatInt(int64(s.CoreM137123.Longitude1), 2)
	for len(str) < 18 {
		str = "0" + str
	}
	Sbinary = Sbinary + str

	str = strconv.FormatInt(int64(s.CoreM137123.Latitude1), 2)
	for len(str) < 17 {
		str = "0" + str
	}
	Sbinary = Sbinary + str

	str = strconv.FormatInt(int64(s.CoreM137123.Longitude2), 2)
	for len(str) < 18 {
		str = "0" + str
	}
	Sbinary = Sbinary + str

	str = strconv.FormatInt(int64(s.CoreM137123.Latitude2), 2)
	for len(str) < 17 {
		str = "0" + str
	}
	Sbinary = Sbinary + str

	str = strconv.FormatInt(int64(s.CoreM137123.StationType), 2)
	for len(str) < 4 {
		str = "0" + str
	}
	Sbinary = Sbinary + str

	str = strconv.FormatInt(int64(s.CoreM137123.ShipAndCargoType), 2)
	for len(str) < 8 {
		str = "0" + str
	}
	Sbinary = Sbinary + str

	str = strconv.FormatInt(int64(s.CoreM137123.Spare2), 2)
	for len(str) < 22 {
		str = "0" + str
	}
	Sbinary = Sbinary + str

	str = strconv.FormatInt(int64(s.CoreM137123.TxRxMode), 2)
	for len(str) < 2 {
		str = "0" + str
	}
	Sbinary = Sbinary + str

	str = strconv.FormatInt(int64(s.CoreM137123.ReportInterval), 2)
	for len(str) < 4 {
		str = "0" + str
	}
	Sbinary = Sbinary + str

	str = strconv.FormatInt(int64(s.CoreM137123.QuietTime), 2)
	for len(str) < 4 {
		str = "0" + str
	}
	Sbinary = Sbinary + str

	str = strconv.FormatInt(int64(s.CoreM137123.Spare3), 2)
	for len(str) < 6 {
		str = "0" + str
	}
	Sbinary = Sbinary + str

	field := strings.SplitN(Sbinary, "", len(Sbinary))

	var encdata = make([]string, int((len(Sbinary)+int(s.FillBits))/6))

	j := 0
	for i := 0; i < int((len(Sbinary)+int(s.FillBits))/6); i++ {

		if i == (int((len(Sbinary)+int(s.FillBits))/6) - 1) {
			for j < len(Sbinary) {
				encdata[i] = encdata[i] + field[j]
				j = j + 1
			}
			for h := 0; h < int(s.FillBits); h++ {
				encdata[i] = encdata[i] + "0" // fill bits
			}
		} else {
			encdata[i] = field[j] + field[j+1] + field[j+2] + field[j+3] + field[j+4] + field[j+5]
			j = j + 6
		}
	}

	var data string
	for j := 0; j < int((len(Sbinary)+int(s.FillBits))/6); j++ {
		i, _ := strconv.ParseInt(encdata[j], 2, 8)
		if i < 40 {
			i = i + 48
		} else {
			i = i + 8 + 48
		}
		data = data + string(rune(i))
	}

	Raw = Raw + "," + data + ","

	if s.FillBitsValidity == true {
		Raw = Raw + strconv.FormatInt(int64(s.FillBits), 10)
	}

	check := Checksum(Raw)

	Raw = Raw + check

	return Raw, nil

}
